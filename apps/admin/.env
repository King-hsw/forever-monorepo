# 打包路径 根据项目不同按需配置
VITE_BASE_URL = /

# 后端地址。留空 = 同源：开发由 vite proxy 转发到 VITE_PROXY_TARGET，生产由 nginx 反代 /api
VITE_API_URL =

# 开发环境代理目标：forever-server（后端无 context-path，端口 8080）
VITE_PROXY_TARGET = http://127.0.0.1:8080