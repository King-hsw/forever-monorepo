# OS specific files
.DS_Store

# dependencies manager
node_modules/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# build files
es/
lib/
dist/
typings/

_site
package
tmp*
temp*
coverage
test-report.html
.idea/
yarn-error.log
*.zip
.history
.stylelintcache

.env.local
.env.*.local

# lockfile 必须入库：CI（.github/workflows/build-admin.yml）用 pnpm install --frozen-lockfile，
# 且 actions/setup-node 的 pnpm 缓存按 apps/admin/pnpm-lock.yaml 定位，缺文件会直接报错。
# 与 apps/web 的做法保持一致（那边也是入库的）。
yarn.lock
