name: 构建镜像 · admin

# admin 之前从未被容器化（没有生产 Dockerfile、也没有任何发布链路）。
# 这一版先做到「构建多架构镜像并推 GHCR」，不复制到 Docker Hub / CCR——
# 那两个 job 直接照 build-web.yml 里的 dockerhub / tencent 抄过来即可，
# 记得把 vars.DOCKERHUB_IMAGE_ADMIN 与 CCR_IMAGE 换成 admin 自己的地址。
on:
  push:
    branches: [main, release]
    tags: ['v*']
    paths:
      - 'apps/admin/**'
      - '.github/workflows/build-admin.yml'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE: ghcr.io/king-hsw/forever-admin

jobs:
  build:
    strategy:
      matrix:
        include:
          - platform: linux/amd64
            runner: ubuntu-latest
            short: amd64
          - platform: linux/arm64
            runner: ubuntu-24.04-arm
            short: arm64
    runs-on: ${{ matrix.runner }}
    # admin 是独立工程：所有 shell 步骤都在它自己的目录里跑
    defaults:
      run:
        working-directory: apps/admin
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      # 锁版本，与 .cnb.yml 和 apps/admin/package.json 的 packageManager 保持一致
      - uses: pnpm/action-setup@v4
        with:
          version: 10.12.1

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: apps/admin/pnpm-lock.yaml

      - name: pnpm install
        run: pnpm install --frozen-lockfile

      # 产物落在 dist/，由本目录的 Dockerfile 拷进 nginx 镜像
      - name: pnpm build
        run: pnpm build

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 构建并按摘要推送
        id: build
        uses: docker/build-push-action@v6
        with:
          # 构建上下文是 apps/admin 自身（nginx 模板已移到 apps/admin/nginx/）
          context: apps/admin
          file: apps/admin/Dockerfile
          platforms: ${{ matrix.platform }}
          outputs: type=image,name=${{ env.IMAGE }},push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha,scope=admin-build-${{ matrix.short }}
          cache-to: type=gha,scope=admin-build-${{ matrix.short }},mode=max

      - name: 导出摘要
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"

      - uses: actions/upload-artifact@v4
        with:
          name: admin-digests-${{ matrix.short }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  merge:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: /tmp/digests
          pattern: admin-digests-*
          merge-multiple: true

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 创建多架构清单并推送
        working-directory: /tmp/digests
        run: |
          TAGS="-t $IMAGE:latest"
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            TAGS="$TAGS -t $IMAGE:${GITHUB_REF#refs/tags/}"
          fi
          # GHCR 最终一致性延迟，带重试
          ok=0
          for i in 1 2 3 4 5 6; do
            if docker buildx imagetools create $TAGS $(printf "$IMAGE@sha256:%s " *); then ok=1; break; fi
            echo "::warning::摘要暂不可读（GHCR 一致性延迟），15s 后重试（$i/6）"
            sleep 15
          done
          [[ $ok == 1 ]] || exit 1

      - name: 检查清单
        run: docker buildx imagetools inspect $IMAGE:latest
